services:
  db:
    image: postgres:16
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: weddingsaas
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
